# linalg

Разработанная в рамках учебного курса "Алгоритмизация и программирование" библиотека для работы с матрицами на C++, предоставляющая широкий набор операций линейной алгебры.

## Автор

**Долгополова Анна Руслановна**  
Студентка 2 курса образовательной программы "Прикладная математика"  
НИУ ВШЭ  
📧 ardolgopolova@edu.hse.ru

## Сборка и запуск

### Требования

- CMake версии 3.22.1 или выше
- Компилятор с поддержкой C++17

### Подключение к проекту

Для использования библиотеки в вашем проекте:

```  bash
git clone https://git.miem.hse.ru/lab-1/linalg.git
cd linalg
mkdir build && cd build
cmake ..
cmake --build .
```


Далее добавьте следующие строки в ваш `CMakeLists.txt`:

```cmake
# Добавление библиотеки как подпроекта
add_subdirectory(path/to/linalg)

# Линковка библиотеки к вашему проекту
target_link_libraries(your_project_name PRIVATE linalg)
```


В исходном коде подключите заголовочный файл:

```cpp
#include <matrix.h>
using namespace linalg;
```

## Структура проекта

```
linalg/
├── include/
│   └── matrix.h          # Заголовочный файл с объявлениями класса Matrix
├── source/
│   └── matrix.cpp        # Реализация методов класса Matrix
├── CMakeLists.txt        # Конфигурация сборки проекта
└── README.md             # Документация проекта
```

## Основные возможности

- Создание и инициализация матриц любого размера
- Арифметика матриц:
    -  сложение матриц
    -  вычитание матриц
    -  перемножение матриц
    -  умножение матрицы на скаляр
    -  операции присваивания с арифметикой (+=, -=, *=)
    -  унарные + и -
    -  сравнение матриц с заданной точностью (== и !=)
- Прямой и обратный ход метода Гаусса
- Вычисление определителя, ранга, нормы
- Транспонирование, возведение в степень, нахождение обратной матрицы
- Решение систем линейных уравнений Ax = b

## Описание методов и принципов работы

### Конструкторы и управление памятью

#### `Matrix()`
Создает пустую матрицу без выделения памяти. Все размеры равны нулю.

#### `Matrix(std::size_t rows)`
Создает вектор-столбец заданной высоты с нулевой инициализацией элементов. Память выделяется динамически как одномерный массив размером `rows`. Размерность матрицы: `rows × 1`.

#### `Matrix(size_t rows, size_t columns)`
Создает матрицу заданного размера с нулевой инициализацией всех элементов. Память выделяется динамически как одномерный массив размером `rows * columns`.

#### `Matrix(const Matrix &other)`
**Конструктор копирования.** Выделяет новую память и копирует все элементы из исходной матрицы. Гарантирует, что изменения в копии не влияют на оригинал.

### `Matrix(Matrix &&other)`
**Конструктор перемещения.** Перехватывает ресурсы временной матрицы без копирования данных.
### `Matrix(std::initializer_list<std::initializer_list<double>> list)`
Cоздает матрицу из двумерного списка для возможности агрегатной инициализации (то есть позволяет пользоваться {}). Размерность определяется количеством внутренних списков (строк) и их длиной (столбцы). Выбрасывает исключение при несовпадении размеров строк.

### `Matrix(std::initializer_list<double> list`)`
Создает вектор-столбец из одномерного списка инициализации. Количество элементов списка определяет количество строк, количество столбцов равно 1.

### `~Matrix()`
**Деструктор.** Освобождает динамически выделенную память. Автоматически вызывается при уничтожении объекта.

#### `reshape(size_t rows, size_t columns)`
Изменяет размеры матрицы. Если новый размер превышает текущую вместимость (`capacity`), происходит реаллокация памяти. При этом старые данные копируются в новый буфер. Метод не уменьшает выделенную память автоматически — для этого используется `shrink_to_fit()`.

#### `reserve(size_t number)`
Резервирует память для заданного количества элементов без изменения текущего размера матрицы.


### Доступ к элементам

#### `operator()(size_t row, size_t col)`
Предоставляет доступ к элементу матрицы по индексам строки и столбца. Элемент `(i, j)` хранится в позиции `i * columns + j` одномерного массива. Выполняет проверку границ и бросает исключение при выходе за пределы матрицы.

### `operator[](std::size_t row)`
Предоставляет доступ к строке матрицы через прокси-объект `Row`, позволяя использовать двумерное индексирование `matrix[i][j]`. Возвращает временный объект, который содержит указатель на начало строки и количество столбцов. Выполняет проверку границ строки.

###  `Row::operator[](size_t col)`
Предоставляет доступ к конкретному элементу в строке. Выполняется как второй этап при использовании синтаксиса `matrix[i][j]`. Выполняет проверку границ столбца и возвращает ссылку на элемент, позволяя как читать, так и изменять значение.


### Операторы присваивания

#### `Matrix &operator=(const Matrix &other)`
**Копирующее присваивание.** Создает копию правой матрицы. Оптимизирует использование памяти: если текущая вместимость достаточна для размещения копируемых данных, перевыделение памяти не происходит. Левая матрица становится идентичной копией правой.

#### `Matrix &operator=(Matrix &&other) noexcept`
**Перемещающее присваивание.** Перехватывает ресурсы временной матрицы. Освобождает существующие ресурсы левой матрицы и перенимает владение памятью правой матрицы.

### Арифметические операции

#### `Matrix operator+() const`
**Унарный плюс.** Возвращает копию текущей матрицы без изменений.

#### `Matrix operator-() const`
**Унарный минус.** Возвращает новую матрицу, где каждый элемент противоположен по знаку соответствующему элементу исходной матрицы. Исходная матрица остается неизменной.

#### `Matrix &operator+=(const Matrix &other)` и `Matrix operator+(const Matrix &other)`
**Поэлементное сложение.** Оператор `+=` модифицирует текущую матрицу, добавляя соответствующие элементы правой матрицы. Требует совпадения размерностей. Оператор `+`создает новую матрицу как сумму двух матриц и реализуется через `+=`.

#### `Matrix &operator-=(const Matrix &other)` и `Matrix operator-(const Matrix &other)`
**Поэлементное вычитание.** Оператор `-=` модифицирует текущую матрицу, вычитая соответствующие элементы правой матрицы. Требует совпадения размерностей. Оператор `-`создает новую матрицу как разность двух матриц и реализуется через `-=`.

#### `Matrix &operator*=(const Matrix &other)` и `Matrix operator*(const Matrix &other)`
**Матричное умножение.** Оператор `*=` выполняет умножение текущей матрицы на правую матрицу и сохраняет результат в текущей матрице (матричное умножение). Требует совпадения количества столбцов левой матрицы с количеством строк правой матрицы. Оператор `*` создает новую матрицу как произведение двух матриц.

#### `Matrix &operator*=(double value)` и `Matrix operator*(double value)` и `Matrix operator*(double value, const Matrix &matrix)`
**Умножение на скаляр.** Оператор `*=` умножает каждый элемент матрицы на скаляр и модифицирует текущую матрицу. Операторы `*` создают новую матрицу с элементами, умноженными на скаляр. Поддерживается как левое, так и правое умножение на скаляр.


### Операторы сравнения

#### `bool operator==(const Matrix &left, const Matrix &right)`
**Проверка на равенство.** Сравнивает две матрицы поэлементно. Сначала проверяет совпадение размерностей - если количество строк или столбцов различается, матрицы считаются не равными. Для вещественных чисел использует сравнение с заданной точностью `EPSILON` (1e-9) для избежания ошибок округления. Возвращает `true` только если все соответствующие элементы равны с учетом численной погрешности.

#### `bool operator!=(const Matrix &left, const Matrix &right)`
**Проверка на неравенство.** Логическое отрицание оператора равенства. Реализован через вызов `operator==`.


### Методы линейной алгебры

#### `double norm() const`
**Норма Фробениуса.** Вычисляет евклидову норму матрицы как корень квадратный из суммы квадратов всех элементов. Определена для матриц любого размера.

#### `double trace() const`
**След матрицы.** Вычисляет сумму элементов на главной диагонали. Определен только для квадратных матриц.

#### `double det() const`
**Определитель матрицы.** Вычисляет детерминант квадратной матрицы, используя прямой ход метода Гаусса (для приведения к верхнетреугольному виду): определитель равен произведению диагональных элементов верхнетреугольной матрицы.

#### `size_t rank() const`
**Ранг матрицы.** Вычисляет максимальное количество линейно независимых строк или столбцов. Использует приведение к ступенчатому виду методом Гаусса и подсчет ненулевых строк.

#### `Matrix &gauss_forward()`
**Прямой ход метода Гаусса.** Приводит матрицу к верхнетреугольному виду. Модифицирует текущую матрицу и возвращает ссылку на неё для цепочки вызовов.

#### `Matrix &gauss_backward()`
**Обратный ход метода Гаусса.** Приводит верхнетреугольную матрицу к главному ступенчатому виду. Модифицирует текущую матрицу.

#### `swap_rows(size_t i, size_t j)`
Обменивает две строки матрицы местами. Используется в методе Гаусса при выборе главного элемента.

#### `swap_count()`
Подсчитывает количество перестановок строк, необходимых при приведении матрицы к ступенчатому виду методом Гаусса.
**Принцип работы**: выполняет симуляцию прямого хода метода Гаусса, увеличивая счетчик при каждой перестановке строк. Используется для вычисления знака определителя.



### Свободные функции

#### `transpose(const Matrix &matrix)`
Транспонирование матрицы — преобразование строк в столбцы и наоборот.
**Принцип работы**: создает новую матрицу размером m×n (если исходная n×m) и копирует элементы:
```
B[j][i] = A[i][j]
```

#### `concatenate(const Matrix &left, const Matrix &right)`
Объединение двух матриц в одну по горизонтали.
**Принцип работы**:
1. Проверяет, что матрицы имеют одинаковое количество строк
2. Создает результирующую матрицу размером n×(m₁+m₂)
3. Копирует строки из левой матрицы, затем из правой
   Используется для формирования расширенной матрицы в методах решения СЛАУ и обращения матриц.

#### `solve(const Matrix &matr_A, const Matrix &vec_f)`
Решение системы линейных алгебраических уравнений (СЛАУ) вида Ax = f методом Гаусса.
**Принцип работы**:
1. Проверяет совместность системы: ранг матрицы A должен совпадать с количеством уравнений
2. Формирует расширенную матрицу [A|f] методом `concatenate`
3. Применяет прямой ход метода Гаусса
4. Применяет обратный ход метода Гаусса
5. Извлекает решение из последнего столбца результирующей матрицы
   Метод работает только для систем с единственным решением.

#### `invert(const Matrix &matrix)`
Вычисление обратной матрицы A⁻¹ такой, что A·A⁻¹ = I.
**Принцип работы**:
1. Формирует расширенную матрицу [A|I], где I — единичная матрица
2. Применяет метод Гаусса (прямой и обратный ход)
3. В результате получается матрица [I|A⁻¹]
4. Извлекает правую половину как обратную матрицу
   Метод работает только для невырожденных квадратных матриц.

#### `power(const Matrix &matr, int power)`
Возведение квадратной матрицы в целую степень.
**Принцип работы**:
- Для степени 0: возвращает единичную матрицу того же размера, что исходная.
- Для положительной степени n: последовательно умножает матрицу на саму себя (n-1) раз
- Для отрицательной степени -n: сначала вычисляет обратную матрицу, затем возводит её в степень n
  **Примечание**: текущая реализация использует наивный алгоритм. Для больших степеней можно оптимизировать методом быстрого возведения в степень, снизив сложность.

#### `create_identity_matrix(size_t length)`
Создание единичной матрицы заданного размера — квадратной матрицы с единицами на главной диагонали и нулями в остальных позициях.
**Принцип работы**: создает нулевую матрицу и устанавливает элементы aᵢᵢ = 1 для всех i.

#### `operator<<(ostream &os, const Matrix &matrix)`
Форматированный вывод матрицы в поток.
**Принцип работы**:
1. Вычисляет максимальную ширину для первого столбца, а также отдельно вычисляет максимальную ширину во всех остальных столбцах, кроме первого.
2. Выводит матрицу в формате с вертикальными разделителями и выравниванием по правому краю
3. Пустая матрица выводит `||`.

## Дальнейшее развитие проекта

### Планируемые улучшения

#### 1. Внедрение дополнительных функций

**Разложения матриц:**
- **LU-разложение**: представление матрицы в виде произведения нижнетреугольной и верхнетреугольной матриц (A = LU). Используется для эффективного решения множества СЛАУ с одной матрицей.
- **QR-разложение**: представление в виде произведения ортогональной и верхнетреугольной матриц (A = QR). Применяется в методе наименьших квадратов и для вычисления собственных значений.
- **SVD (сингулярное разложение)**: A = UΣVᵀ, где U и V — ортогональные, Σ — диагональная. Универсальный инструмент для анализа матриц, сжатия данных, решения некорректных задач.
- **Разложение Холецкого**: для положительно определенных матриц A = LLᵀ. Быстрее LU-разложения.

**Собственные значения и векторы:**
- Степенной метод для нахождения максимального собственного значения
- QR-алгоритм для всех собственных значений
- Метод обратных итераций для конкретного собственного вектора

**Метод наименьших квадратов:**
- Решение переопределенных систем (больше уравнений, чем неизвестных)

#### 2. Повышение эффективности умножения матриц
- **Алгоритм Штрассена:**
  Рекурсивный алгоритм, снижающий сложность умножения матриц n×n с O(n³) до O(n^2.807) за счет уменьшения количества операций умножения с 8 до 7 при разбиении матриц на блоки.

#### 3. Шаблонизация класса Matrix
- Преобразование класса в шаблон для поддержки различных числовых типов.

**Преимущества:**
- Поддержка различной точности вычислений (float для экономии памяти, long double для высокой точности)
- Возможность работы с комплексными числами
- Интеграция пользовательских числовых типов

#### 4. Расширение функциональности
- Поддержка комплексных чисел

## Известные ограничения

- Отсутствие проверки переполнения при арифметических операциях
- Наивная реализация умножения (O(n³))
- Нет поддержки многопоточности
## Контакты

По всем вопросам обращайтесь:
- 📧 Email: ardolgopolova@edu.hse.ru

---

**Версия документа**: 1.0  
**Последнее обновление**: Октябрь 2025